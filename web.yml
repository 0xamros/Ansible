---
- hosts: web
  remote_user: amr
  become: yes
  tasks:
    - name: update_cache
      apt:
        update_cache: yes
    - name: install apache
      apt:
        name: apache2
        state: present